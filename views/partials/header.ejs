<!DOCTYPE html>
<html>
    <head>
        <title>Anish's Personal Site</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
        <link rel="stylesheet" type = "text/css" href ="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.1.4/semantic.min.css">
        <link rel="stylesheet" href="/stylesheets/main.css">
        <link rel="stylesheet" href="/stylesheets/app.css">
    </head>
    <body>
    <!-- Include the nav bar -->
    <nav class = "navbar navbar-expand-sm bg-dark navbar-dark">
        <div class = "container-fluid">
            <div class = "navbar-header">
                <a class="navbar-brand" href ="/">Landing Page</a>
                  <!--div class = "nav collapse navbar-collapse"-->
                    <ul class="nav navbar-nav justify-content-right">
                        <% if(currentUser === undefined){ %>
                            <li><a href = "/login" class="nav navbar-nav justify-content-right">Login</a></li>
                            <li><a href = "/signup" class="nav navbar-nav justify-content-right">Sign Up</a></li>
                        <% }else{ %>
                            <li><a href="#">Welome : <%=currentUser.username %></a></li>
                            <li><a href = "/home" class="nav navbar-nav justify-content-right">Home</a></li>
                            <li><a href = "/logout" class="nav navbar-nav justify-content-right">Logout</a></li>
                        <% } %>
                    </ul>
                <!--/div-->
            </div>
        </div>
    </nav>
    <div class="container">
        <% if(error && error.length > 0 ){ %>
            <div class = "alert alert-danger" role ="danger">
                <%= error%>    
            </div>         
        <% } else if(success && success.length > 0){ %>
            <div class = "alert alert-success" role ="danger">
                <%= success%>    
            </div> 
         <% } %>
    </div>
    
